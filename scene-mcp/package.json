{
  "name": "scene-mcp",
  "version": "0.1.0",
  "description": "MCP server for three.js + Spline + rive + matter.js scene rendering for PPT generation",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "start": "npx tsx src/index.ts",
    "test": "npx tsx --test src/**/*.test.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.9.0",
    "puppeteer": "^24.0.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
